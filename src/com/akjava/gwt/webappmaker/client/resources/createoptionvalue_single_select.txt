String[] option_value_${key}={${option_value}};
String value_${key}=map.get("${key}");
List<String> multipleValue=Lists.as(xxx.split(":"));//or ,
//TODO manually after
int opIndex=0;
for(String option:option_value_${key}){
	String mkey="selected_${key}"+opIndex;
	if(multipleValue.contains(option)){
	map.put(mkey,"selected");
	}else{
	map.put(mkey,"");
	}
	opIndex++;
}