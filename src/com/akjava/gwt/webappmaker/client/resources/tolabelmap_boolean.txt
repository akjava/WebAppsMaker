	if ("${key}".equals(key)) {
			if(value==null){
				return "${false_value}";
				}
			if(value.toLowerCase().equals("on")){//true must be never happen
				return "${true_value}";
				}
			else{
				return "${false_value}";
				}
		}